# NEON BLOCKS

ブラウザだけで遊べる、依存関係なしのテトリス風Webゲームです。

## 起動

`index.html` をブラウザで開くか、ローカルサーバーを起動します。

```sh
python3 -m http.server 8000
```

その後、`http://localhost:8000` を開いてください。

## 操作

- `←` / `→`: 移動
- `↑`: 回転
- `↓`: ソフトドロップ
- `Space`: ハードドロップ
- `P`: 一時停止
